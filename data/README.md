# data/

CSVs de La Liga 2025-26 (FBref vía `soccerdata`, Understat para xG).

- `raw/` — descargas crudas (gitignored).
- `processed/` — `teams.csv`, `players.csv`, `matches.csv` (gitignored).
- `samples/` — muestras pequeñas versionadas para que el repo sea reproducible.

Script de descarga: `src/data/fetch.py` (pendiente).
